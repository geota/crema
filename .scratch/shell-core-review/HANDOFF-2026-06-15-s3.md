# Handoff — session 3 (post Themes 2–5 kickoff)

**Updated:** 2026-06-15 (session 3) · **Branch:** `effect/phase-0-spike` ·
supersedes [HANDOFF-2026-06-15.md](HANDOFF-2026-06-15.md) (session 2).

## TL;DR

Worked the holistic-review backlog (`issues/INDEX.md`), one commit per issue,
flipping each issue file + INDEX + a dated note. **9 issues closed across 3 of
the 4 operator clusters** (Android consolidation, web parity, core); **3 big
Android dedups deferred with detailed scoping notes** (entangled / unverifiable
without hardware). Build green at HEAD (Android `:app:compileDebugKotlin` +
`:app:testDebugUnitTest`; web `vitest` + `svelte-check` 0/0).

Emulators (phone `5554`, tablet `5556`) still seeded + on the latest debug APK.

## Done this session (committed, newest first)

| Commit | Issue | What | Verified |
|--------|-------|------|----------|
| `47dab7a` | **24** | core `brand::mark_svg` — already documented (generated module doc covers it); recorded, no code change | grep (only def/tests/generator) |
| `d067999` | **17** | web — rewrote 4 stale brew/scale "UI-only / TODO: wire to DE1" headers to shipped reality (verified control IS wired first) | grep (0 stale claims) |
| `af52063` | **47** | web — `.pp-card-roast` `uppercase`→`capitalize` (+ drop all-caps tracking): "Light/Medium/Dark" | svelte-check; deterministic CSS |
| `2fd922a` | **43** | web — one `relativeAgo(ms,asOf)` (`$lib/utils/relative-time`); `relativeLastUsed` delegates; `ShotRow.ago` adopts it. **Closes 43** (Android shipped session 2) | vitest (21 cases + quirk) + svelte-check |
| `fa325f4` | **27** | Android — `SettingsConfirmDialogs` state-holder + composable (7 dialogs + SAF) shared phone↔tablet | tablet emulator (reset dialog) |
| `12319f2` | **38** | Android — phone 18dp cards → `CremaCardSpec.phoneRadius` (5 surfaces) | phone emulator (profiles + beans) |
| `717fddc` | **31** | Android — `applyBeanEdits(bean, draft)` + one `BAG_PRESETS` | compile + tests |
| `3e60270` | **30** | Android — `MaintenanceBurndown` (filterRow/descaleRow/cleanRow) | tablet emulator (reminders) |
| `ced5883` | **34** | Android — `CremaSparkChart` (was `SparkChart`≡`PhoneSpark`) | both emulators (history) |

INDEX now: T1 done; T2 done (17 ✅); T3 — 43 ✅, 47 ✅, remaining 45/50; T4 —
27/30/31/34/36/38 ✅, remaining 26/28/32/33/35/37/39/40/41; T5 — 24 ✅,
remaining 18–23/25.

## Deferred this session (read the issue-file notes — they have the analysis)

- **35 — steppers → `CremaStepper`** (`ready-for-agent`). Big variant-unification:
  8 reimpls in **two API shapes** (numeric vs value-string+onMinus/onPlus); the
  reimpls are compact/boxed (28–32dp Surface buttons, JetBrainsMono) vs the 56dp
  `CremaStepper`. **Entangled:** issue 26 also rehomes `SetStepper`/`PStepper`;
  issue 50's inline `>`/`<` is `EditStepper`'s `compareSymbol`. **Do 35 + 26 + 50
  together, one focused session.** Prereq for 44-Category-B.
- **26 — three settings-row systems** (`ready-for-agent`). 6+6 private composables
  (`SetRow/SetPill/...` ≡ `PRow/PPill/...`); shares the steppers with 35 — do
  together so they aren't unified twice.
- **28 — filter/sort + `effectiveBrew`** (`ready-for-agent`). **Now pure dedup**
  — the correctness fixes it claimed (issue-16 All-filter, issue-01 ratio) are
  already landed. Split into 4 parts (see note): `effectiveBrew` (3-way
  brewParams?:profile?:18/36/93 — careful NOT to fold in the unrelated
  `seg.temp ?: 93f` segment defaults or the 2-way `active?.dose ?: 18f`), beans
  `filterAndSortBeans`+`beanFilterCounts` (predicate already aligned; the drift
  left is the filter-chip COUNTS), history predicate, profiles predicate.
- **33 — scaleMeta + capability body** (`ready-for-agent`, do after 26+35).
  `scaleMeta` is a trivial shared fn; the capability body is a real design-merge
  (mapped vs core caps, ToggleRow/SegRow vs SettingsRow, tablet omits the
  display-mode row) and is **unverifiable on the emulators (no scale simulator)**.
  Decide the tablet-modes-row question; verify behind real hardware.

## Recommended next sequence

1. **Android focused session:** 35 + 26 (+ 50) together → then 33 → then 28
   (4 parts) → 32 (segment unit helpers, with the editor) → 44-Category-B
   (unit-aware steppers, the deferred half of 44) → 37 (empty-state; the clean
   sub-win is the ScaleScreen `titleLarge` vs phone type-scale nit).
2. **Web:** 39 (`useVisualizerConnection` rune), 40 (`bestEffortRemoteDelete`),
   41 (`useStepper` numeric core), 45 (design tokens). **New sibling follow-up
   noticed:** Profiles/Beans/History each carry their own SAF `launchSave`
   launcher (same shape as the issue-27 Settings one) — worth a "share SAF
   export launcher" issue.
3. **Core Rust (focused, mind the rustfmt-skew rule + clippy gate):** 19
   (`f64_to_ms`), 20 (scale receivers `&mut`→`&`), 21 (line-freq cast), 22
   (warmup clamp), 23 (`RoastBand::from_wire_str`), 25 (`core_version` &'static
   str). 18 (FFI parity: calibration-write / reset-defaults) changes the FFI
   surface → needs binding regen on both shells.
4. **29 RESCOPED (research first):** low-tank warning → a Settings control seeded
   from the DE1 firmware default refill threshold; research the de1app/Decent
   default + decide machine-write vs shell-only. Drop `LOW_TANK_MM` 5f /
   `LOW_TANK_MM_PHONE` 20f.

## House rules (carried over — important)

- **Never `git add -A`.** Stage explicit paths. `.scratch/android-compose-polish/`,
  `.scratch/phone-impl/`, `Task`, `seed/gen_seed.py` are the USER's — never commit.
- `cd X && git …` does NOT persist cwd between Bash calls — run `git` from the repo
  root. (Minor slip this session: INDEX.md status notes for deferred issues got
  swept into adjacent code commits since INDEX was staged while carrying them —
  harmless, all review-tracker text; not worth rewriting history.)
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **rustfmt skew:** local rustfmt 1.9.0 reformats untouched files — never
  `cargo fmt --all`; hand-fix only your own >100-col lines.
- One commit per issue; flip issue file status + INDEX + dated note. INDEX edit
  gotcha: match the *full* row incl. trailing status.
- **No scale/DE1 simulator** — anything needing a connected scale (the 33
  capability body) or DE1 write is build + no-crash + shell-logic only. Verify
  loop: `./gradlew :app:compileDebugKotlin :app:testDebugUnitTest`; install
  `adb -s emulator-5556 install -r -d <apk>`; web `pnpm exec vitest run` +
  `pnpm exec svelte-check`.
