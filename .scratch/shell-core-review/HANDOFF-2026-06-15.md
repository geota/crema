# Handoff — Themes 2–5 (post-T1)

**Updated:** 2026-06-15 (session 2) · **Branch:** `effect/phase-0-spike` ·
supersedes the T1 view in [HANDOFF.md](HANDOFF.md) (T1 01–11 all done/deferred).

## TL;DR

Working the holistic-review backlog in `.scratch/shell-core-review/issues/`, one
commit per issue, flipping each issue file + `issues/INDEX.md` + a dated note.
Emulators verify everything not gated on a real DE1 (**phone `emulator-5554`**,
**tablet `emulator-5556`**, via adb). **No DE1 simulator** — anything that writes
to the machine over BLE is build + no-crash + shell-logic only. Both emulators are
seeded (seed.sh) and currently have the latest debug APK installed.

## Done this session (committed, newest first)

| Commit | Issue | What | Verified |
|--------|-------|------|----------|
| `7c95e0a` | **49** | Phone `beanLine` "frozen" → "Frozen" (title-case, matches tablet). The lowercase verdict key is untouched | compile |
| `702f2b9` | **36** | `CremaStarRating(...)` in CremaComponents; replaced **8** hand-rolled star rows (tablet + phone) + removed 2 private defs + dropped unused imports | tablet emulator (list + detail ratings) |
| `ba75007` | **46** | Bean-freshness hex → `FreshnessPalette` (Color.kt) + `CremaFreshnessColors` theme local + `CremaTheme.freshness`; `freshnessColor` now `@Composable`. Format-standardization half was already done by #44 | compile (value-identical refactor) |
| `5fd5d7a` | **44** | °C/°F · g/oz · bar/psi · ml/floz toggles **end-to-end**: MainUiState units + round-trip + 4 setters; readout sweep across all brew/scale/history/profile **display** readouts via `Format.kt`; Settings toggles wired both shells | tablet + phone emulator (flip → readouts convert) |
| `126f19c` | 44 (WIP) | Prior session: UniFFI 8 conversions + `Format.kt` + `AppPrefs` unit fields | — |

Build green at HEAD: `:app:compileDebugKotlin` + `:app:testDebugUnitTest` SUCCESSFUL.

## Decisions banked this session (read before touching the deferred items)

- **44 scope split.** Implemented all **display readouts** + the Settings toggles
  (the issue's written acceptance). **Deferred: unit-aware editable steppers**
  (Quick Controls steam/water/flush temp+vol, dose/yield, profile-editor segment
  temp/pressure/volume, bean bag/remaining, Settings brew defaults). The web makes
  these unit-aware via a `dimension`-prop stepper with display-grid stepping +
  inverse conversion; porting across the ~6 bespoke Android stepper helpers is a
  larger change. They still commit canonical, so deferring is **cosmetic, not a
  correctness gap.** Worth its own issue. Also deferred: the hardcoded mode-chip
  labels ("148 °C · 90 s") — they don't reflect live QC state yet (issue 14/50).
- **46 compact-phone exception.** Dense phone readouts (4-up machine-readiness
  strip, profile-card metrics, history row/detail) render converted value+unit
  **compact, no space** ("18.0g", "93.0°C") to fit; glyph still explicit. Spacing
  them to the tablet's "18.0 g" risks clipping — left as a layout follow-up.

## Deferred this session — need operator input (do NOT guess)

- **43 — one relative-time vocabulary.** Web half is clean (unify `ShotRow.svelte`
  + `profiles/model.ts` into one helper). **Android half is ambiguous:** the
  decision says "keep day-section headers independent," but the phone's ONLY
  relative-time surface IS its day-bucket section headers (the row shows HH:mm, no
  per-row "ago"). The acceptance ("a 2h shot reads '2h ago' on phone History")
  conflicts with keeping the phone's bucket headers. **Q for operator:** does the
  phone gain a per-row "Nh ago" (replacing/added to HH:mm), or do its day-section
  headers satisfy it? Canonical scale already shipped in `profiles/model.ts`
  `relativeLastUsed` (just now / Nm / Nh / Nd / Nw<5 / Nmo<12 / Ny) — match it.
- **29 — low-tank threshold (phone 20f vs tablet 5f).** Consolidation is clean but
  the **value is a UX/hardware call**: 20mm warns earlier (safer vs running dry
  mid-shot), 5mm later. No web/core reference resolves it (web uses the DE1's own
  `refill_threshold`, a machine-side concept, not a shell UI warning). **Q for
  operator:** which mm threshold is hardware-correct? Then define once (e.g.
  `CremaConstants.kt`) + reference from both.

## Other clean-ish backlog (no operator input, but has wrinkles)

- **37 — `CremaEmptyState`.** Sites are **heterogeneous** (full-page centered w/
  CTA button vs card-embedded w/ Eyebrow vs "pair a scale" hero), so one component
  risks changing layouts. The clear sub-win: fix the type-scale mismatch
  (`ScaleScreen` `titleLarge` vs `PhoneScaleScreen` `titleSmall`). Needs a careful
  API (message + optional action + optional icon) or per-shape variants.
- **32 — `SegmentEdit` unit helpers + promote `toEdit()`.** Clean consolidation
  (9 `isPressure→"bar"/"ml/s"` sites + un-inline `toEdit`), but touches the
  profile editor — coordinate with the deferred **44 Category-B** stepper work
  (the editor is where most editable-stepper unit-awareness would land).
- Remaining: Theme 4 (26–28, 30, 31, 33–35, 38–41), Theme 5 core Rust (18–25 —
  **rustfmt-skew hazard**, see house rules), Theme 3 (17, 45, 47, 50).
  `35` (stepper → `CremaStepper`) is a prerequisite that would make 44-Category-B
  much easier — consider doing it before the stepper unit-awareness issue.

## House rules (carried over — important)

- **Never `git add -A`.** Stage explicit paths. Untracked
  `.scratch/android-compose-polish/`, `.scratch/phone-impl/`, `Task`, and
  `seed/gen_seed.py` are the USER's — never commit them.
- **`cd X && git …` does NOT persist cwd** between Bash calls here — run `git`
  from the repo root (`/Users/adrianmaceiras/code/crema`), not after a `cd android`.
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **rustfmt skew:** local rustfmt 1.9.0 reformats untouched files — never
  `cargo fmt --all`; hand-fix only your own >100-col lines.
- One commit per issue; flip the issue file status + INDEX + append a dated note.
  **INDEX edit gotcha:** match the *full* row (incl. trailing status) or you'll
  double the status cell.
- Verify loop: `./gradlew :app:compileDebugKotlin` + `:app:testDebugUnitTest`;
  install via `adb -s emulator-5556 install -r -d <apk>`; `input tap` uses
  **physical** coords — read bounds from `uiautomator dump`, not screenshot px
  (tablet 2560×1600; phone 1280×2856). Prefs persist across reinstall (file-JSON).
