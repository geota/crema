# Handoff — Themes 2–5 (post-T1)

**Updated:** 2026-06-15 · **Branch:** `effect/phase-0-spike` · supersedes the T1
view in [HANDOFF.md](HANDOFF.md) (T1 01–11 all done/deferred there).

## TL;DR

Working the holistic-review backlog in `.scratch/shell-core-review/issues/`, one
commit per issue, after the four `ready-for-human` issues were decided. Decided +
P1 issues are being implemented; emulator (phone `emulator-5554` + tablet
`emulator-5556`, via MCP/adb) is used to verify everything not gated on a real
DE1. **There is no DE1 simulator** — anything that writes to the machine over BLE
is "DE1-gated": build + no-crash + shell-logic only.

## Done since T1 (committed, newest first)

| Commit | Issue | What | Verified |
|--------|-------|------|----------|
| `d93eae2` | **15** | Android grind (persist+record onto `StoredShot.grindSetting`), preinf (transient override capping the leading segment via `overrideBrewParamsJson`), pre-flush/steam-purge consumers (`proceedToEspresso` + `scheduleAutoPurge`); removed the 4 now-false "Not implemented" pills | tablet: grind→4.4 persists; preinf→8s resets to profile 2s; `:app:testDebugUnitTest`; no crashes |
| `5cc7e00` | **14** | QuickControls steam/water/flush **persist + RMW to machine**, Android + web. Android: 7 prefs + `ShotSettingsRead` cache + `buildSteamHotWaterSettings` + JVM tests. Web: `setSteamHotwaterSettings` facade + `App.setSteamHotwater` RMW + `qc*` settings + `BrewParamState.qcSeed` (untracked) | tablet: changed + survived restart; web `npm run check` clean |
| (earlier this run) | 48, 42, 16, 12, 13 | History stats canonical set; phone roast→copper; phone wiring regressions; calibration pills; web `visualizerAutoUpload` toggle | per issue files |

Also filed **issue 50** (phone segment-options → match QuickControls selectors,
inline the `>`/`<`) — not implemented.

## Decisions banked (from the `ready-for-human` batch + follow-ups)

- **15 grind** → persist the QC dial + record onto the shot (diverges from web,
  which records `bean.grinderSetting`). Done.
- **15 preinf** → **transient per-shot override** capping the leading pre-infuse
  segment. Confirmed via core/protocol that the DE1 has *no* pre-infusion
  register — pre-infusion is just the leading frame(s) (`NumberOfPreinfuseFrames`
  tags them; `blank_profile` sets `segments.first().time = preinfusion_s`). So a
  preinf value is duplicative of the segment; the override only re-caps frame 0.
- **14** → persist QC steam/water/flush + RMW; no global Settings UI; not
  profile-scoped. Done both shells.
- **44** → implement Android unit conversion (decided, NOT hide). See below.
- **43** → one compact relative-time vocabulary per shell (decided). Not started.

## NEXT — in progress / queued

### Issue 44 — Android °C/°F & g/oz unit toggles (decided: implement) — LARGE
Gating fact: `core/de1-domain/units.rs` has all 8 conversions; **wasm exports
them, UniFFI did NOT** (now exported — see WIP commit below). Plan:
1. ✅ **DONE (WIP commit `<<see git log: "wip: issue 44 scaffolding">>`):**
   `#[uniffi::export]` the 8 conversions in `core/de1-ffi/src/lib.rs`
   (`grams_to_oz`, `oz_to_grams`, `celsius_to_fahrenheit`, `fahrenheit_to_celsius`,
   `bar_to_psi`, `psi_to_bar`, `ml_to_fl_oz`, `fl_oz_to_ml`) — they generate as
   camelCase top-level Kotlin fns in `coffee.crema.core` (`gramsToOz`, …); binding
   regenerates at gradle build (no committed binding to hand-edit).
2. ✅ **DONE (same WIP commit):** central Kotlin formatter in
   `android/.../ui/Format.kt` — `Measurement` + `convert{Weight,Temp,Pressure,
   Volume}(canonical, unit)` + `format*` joiners, mirroring web
   `$lib/settings/format` exactly ("18.0 g", "93.0 °C", "9 bar"→psi rounds, "250
   ml"). Unit strings = web vocab (`"g"/"oz"`, `"C"/"F"`, `"bar"/"psi"`,
   `"ml"/"floz"`). **Not yet called anywhere.**
3. 🟡 **PARTIAL (same WIP commit):** `AppPrefs` has `weightUnit`/`tempUnit`/
   `pressureUnit`/`volumeUnit` (defaults `"g"/"C"/"bar"/"ml"`). **STILL TODO:**
   add the same 4 to `MainUiState` (insertion point: after `chartChannels`,
   `MainViewModel.kt:300`) + currentPrefs/loadPrefs round-trip + 4 setters
   (mirror the issue-14 `setQc*` setters — update UiState + `persistPrefs()`).
4. ⬜ **TODO — the bulk:** sweep every brew/scale/history readout through the
   formatters (BrewScreen, QuickControlsSheet, HistoryScreen, ScaleScreen + phone
   variants). Grep for `°C`, `" g"`, `formatTemp`-less temp/weight string-building.
5. ⬜ **TODO:** wire the toggles — tablet `SettingsScreen.kt:547-552`, phone
   `PhoneSettingsScreen.kt:615-620`: drop `notImplemented`, make them real selects
   bound to the prefs + setters.
Fully emulator-verifiable (flip toggle → seeded readouts change; no DE1).
Build green at WIP commit: `:app:compileDebugKotlin` BUILD SUCCESSFUL.

### Issue 43 — One relative-time vocabulary per shell (decided: compact relative)
Smaller. Standardize "5m ago / 3h ago / 2d ago"-style across each shell's
relative-time renderers. Check `INDEX.md` Theme 3 + the issue file for sites.

### Remaining backlog
INDEX.md Themes 4 (intra-shell consolidation 26–41), 5 (core-internal Rust
18–25), 3 (design nits 17, 45–47, 49, 50). All `ready-for-agent`, none decided
beyond 43/44.

## House rules (carried over — important)

- **Never `git add -A`.** Stage explicit paths. Untracked
  `.scratch/android-compose-polish/`, `.scratch/phone-impl/`, `Task`, and
  `seed/gen_seed.py` are the USER's — never commit them.
- Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- **rustfmt skew:** local rustfmt 1.9.0 reformats untouched files — never
  `cargo fmt --all`; hand-fix only your own >100-col lines.
- One commit per issue; flip the issue file status + INDEX + append a dated note.
- Verify loop: `./gradlew :app:compileDebugKotlin` + `:app:testDebugUnitTest`;
  install via `adb -s emulator-5556 install -r -d <apk>`; `input tap` uses
  **physical** coords (tablet 2560×1600, 4× the 640-wide screenshot) — read bounds
  from `uiautomator dump`, not screenshot estimates.
