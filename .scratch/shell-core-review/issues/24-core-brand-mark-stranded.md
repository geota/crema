# 24 — Document or remove stranded `brand::mark_svg()`

- **Status:** ✅ done (2026-06-15) — already satisfied
- **Severity:** P3
- **Area:** core — `de1-domain/src/brand.rs`
- **Punchlist:** T5-07 — `../PUNCHLIST.md`
- **Depends on:** none

## Problem
`core/de1-domain/src/brand.rs:27-40` are `pub` but called by neither bridge;
assets are pre-generated by `brand/generate_mark.py`.

## Fix
Add a `// build-tool only — assets pre-generated` module note, or expose via WASM
if runtime generation is wanted.

## Acceptance / Verify
- Either: `brand.rs` has a module-level doc comment explaining that `mark_svg()` / `mark_maskable_svg()` are build-tool-only and assets are pre-generated by `brand/generate_mark.py`.
- Or: the functions are exposed via the WASM binding with an intentional use case documented.

## Touched files
- `core/de1-domain/src/brand.rs:27-40` — add doc comment or expose via WASM binding

## Comments
<!-- triage + progress notes append below -->

### 2026-06-15 — already satisfied (no code change)
Acceptance option 1 is already met. `core/de1-domain/src/brand.rs:1-7` carries a
module-level doc comment:

> Crema brand mark — GENERATED by `brand/generate_mark.py`; do not edit by hand.
> … The web (`static/favicon.svg`, PWA icons) and Android (`ic_crema_logo.xml`)
> generate their committed assets from this same path. Kept as plain domain data
> (no FFI export) until a shell renders it at runtime.

This explains the generated-ness + why `mark_svg()`/`mark_maskable_svg()` are
uncalled (retained as domain data for a future runtime renderer, assets
otherwise pre-generated) — clearer than the issue's "build-tool-only" framing
(the Python generator *emits* `brand.rs` from a template; it doesn't *call* the
Rust fns, and the fns are exercised only by their own `#[cfg(test)]`).

Note: `brand.rs` is generated, so the doc correctly lives in the generator's
`_RUST_TEMPLATE` (`generate_mark.py:82`) and is reproduced into `brand.rs` —
editing `brand.rs` by hand would be overwritten. Nothing to change; no commit of
core code. Verified `grep mark_svg` → only the def, its own tests, and the
generator template (no bridge/runtime caller).
