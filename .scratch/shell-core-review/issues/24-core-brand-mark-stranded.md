# 24 — Document or remove stranded `brand::mark_svg()`

- **Status:** ready-for-agent
- **Severity:** P3
- **Area:** core — `de1-domain/src/brand.rs`
- **Punchlist:** T5-07 — `../PUNCHLIST.md`
- **Depends on:** none

## Problem
`core/de1-domain/src/brand.rs:27-40` are `pub` but called by neither bridge;
assets are pre-generated by `brand/generate_mark.py`.

## Fix
Add a `// build-tool only — assets pre-generated` module note, or expose via WASM
if runtime generation is wanted.

## Acceptance / Verify
- Either: `brand.rs` has a module-level doc comment explaining that `mark_svg()` / `mark_maskable_svg()` are build-tool-only and assets are pre-generated by `brand/generate_mark.py`.
- Or: the functions are exposed via the WASM binding with an intentional use case documented.

## Touched files
- `core/de1-domain/src/brand.rs:27-40` — add doc comment or expose via WASM binding

## Comments
<!-- triage + progress notes append below -->
